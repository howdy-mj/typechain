const name = "MJ";
const age = 25;
const gender = "female";

const sayHi = (name, age, gender) => {
  console.log(`Hello ${name}, you are ${age} and ${gender}`);
};

sayHi(name, age, gender);

export {};
